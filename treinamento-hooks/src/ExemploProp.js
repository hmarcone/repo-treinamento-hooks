import React from 'react';

const ExemploProp = ({ nome }) => {
    return (
        <>
            <h1>Exemplo de props...</h1>
            <span>{nome}</span>
        </>
    );
}

export default ExemploProp;